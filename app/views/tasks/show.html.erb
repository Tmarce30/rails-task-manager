<h2><%= @task.name %></h2>
<p><%= @task.details %></p>
<p><%= @task.finished %></p>
<%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<%= link_to "update", edit_task_path %>

<h3><%= link_to "home", tasks_path %></h3>
